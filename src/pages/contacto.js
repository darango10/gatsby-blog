import React from 'react';
import Layout from "../components/Layout";

const Contacto = () => {
    return (
        <Layout>
            Contacto
        </Layout>
    );
};

export default Contacto;
